entityTypes:
  - name: "behavior"
    requiredFields: ["id"]
  - name: "behavior" # Duplicate name
    requiredFields: ["id", "other"]

backends:
  - type: localfs
    settings:
      rootDir: "."
      entityLocations:
        behavior: "*.bhv"