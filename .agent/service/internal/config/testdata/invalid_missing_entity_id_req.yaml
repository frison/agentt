entityTypes:
  - name: "behavior"
    requiredFields: ["title", "description"] # Missing 'id'

backends:
  - type: localfs
    settings:
      rootDir: "."
      entityLocations:
        behavior: "*.bhv"